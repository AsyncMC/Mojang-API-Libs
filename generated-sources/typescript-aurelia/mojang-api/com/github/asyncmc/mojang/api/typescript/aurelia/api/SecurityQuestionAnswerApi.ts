/**
 * Mojang API
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * OpenAPI spec version: 2020-06-05
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { autoinject } from 'aurelia-framework';
import { HttpClient } from 'aurelia-http-client';
import { Api } from './Api';
import { AuthStorage } from './AuthStorage';
import {
  SecurityChallenge,
} from './models';

/**
 * checkSecurityStatus - parameters interface
 */
export interface ICheckSecurityStatusParams {
}

/**
 * listPendingSecurityQuestions - parameters interface
 */
export interface IListPendingSecurityQuestionsParams {
}

/**
 * sendSecurityQuestionAnswers - parameters interface
 */
export interface ISendSecurityQuestionAnswersParams {
  securityAnswer: Array<SecurityAnswer>;
}

/**
 * SecurityQuestionAnswerApi - API class
 */
@autoinject()
export class SecurityQuestionAnswerApi extends Api {

  /**
   * Creates a new SecurityQuestionAnswerApi class.
   *
   * @param httpClient The Aurelia HTTP client to be injected.
   * @param authStorage A storage for authentication data.
   */
  constructor(httpClient: HttpClient, authStorage: AuthStorage) {
    super(httpClient, authStorage);
  }

  /**
   * Check if security questions are needed
   */
  async checkSecurityStatus(): Promise<any> {
    // Verify required parameters are set

    // Create URL to call
    const url = `${this.basePath}/user/security/location`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'PlayerAccessToken' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Get list of questions
   */
  async listPendingSecurityQuestions(): Promise<Array<SecurityChallenge>> {
    // Verify required parameters are set

    // Create URL to call
    const url = `${this.basePath}/user/security/challenges`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'PlayerAccessToken' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Send back the answers
   * @param params.securityAnswer An array with all the answers
   */
  async sendSecurityQuestionAnswers(params: ISendSecurityQuestionAnswersParams): Promise<any> {
    // Verify required parameters are set
    this.ensureParamIsSet('sendSecurityQuestionAnswers', params, 'securityAnswer');

    // Create URL to call
    const url = `${this.basePath}/user/security/location`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()
      // Encode body parameter
      .withHeader('content-type', 'application/json')
      .withContent(JSON.stringify(params['securityAnswer'] || {}))

      // Authentication 'PlayerAccessToken' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

}

