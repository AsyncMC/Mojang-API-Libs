# Mojang API
#
# No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
#
# OpenAPI spec version: 2020-06-05
# 
# Generated by: https://openapi-generator.tech

#' @title SkinOperations operations
#' @description openapi.SkinOperations
#'
#' @field path Stores url path of the request.
#' @field apiClient Handles the client-server communication.
#' @field userAgent Set the user agent of the request.
#'
#' @importFrom R6 R6Class
#'
#' @section Methods:
#' \describe{
#'
#' change_player_skin Changes the player skin by URL
#'
#'
#' reset_player_skin Resets the player skin to default
#'
#'
#' upload_player_skin Changes the player skin by upload
#'
#' }
#'
#' @export
SkinOperationsApi <- R6::R6Class(
  'SkinOperationsApi',
  public = list(
    userAgent = "OpenAPI-Generator/1.0.0/r",
    apiClient = NULL,
    initialize = function(apiClient){
      if (!missing(apiClient)) {
        self$apiClient <- apiClient
      }
      else {
        self$apiClient <- ApiClient$new()
      }
    },
    change_player_skin = function(stripped_uuid, url, model, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      body <- list(
          "model" = model,
          "url" = url
      )

      urlPath <- "/user/profile/{stripped_uuid}/skin"
      if (!missing(`stripped_uuid`)) {
        urlPath <- gsub(paste0("\\{", "stripped_uuid", "\\}"), `stripped_uuid`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        # void response, no need to return anything
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    reset_player_skin = function(stripped_uuid, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      urlPath <- "/user/profile/{stripped_uuid}/skin"
      if (!missing(`stripped_uuid`)) {
        urlPath <- gsub(paste0("\\{", "stripped_uuid", "\\}"), `stripped_uuid`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "DELETE",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        # void response, no need to return anything
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    upload_player_skin = function(stripped_uuid, file, model, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      body <- list(
          "model" = model,
          "file" = httr::upload_file(file)
      )

      urlPath <- "/user/profile/{stripped_uuid}/skin"
      if (!missing(`stripped_uuid`)) {
        urlPath <- gsub(paste0("\\{", "stripped_uuid", "\\}"), `stripped_uuid`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "PUT",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        # void response, no need to return anything
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    }
  )
)
